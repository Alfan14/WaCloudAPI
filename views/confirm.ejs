<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration - Confirm</title>
    <style>body{font-family: sans-serif; margin: 20px;} .data-item {margin-bottom: 10px;} .data-item strong{display: inline-block; width: 100px;} button{padding: 10px 15px; background-color: #28a745; color: white; border: none; cursor: pointer;} .actions a{margin-right: 15px;}</style>
</head>
<body>
    <h1>Registration - Confirm Details</h1>
    <% if (formData) { %>
        <div class="data-item"><strong>Name:</strong> <%= formData.applicant_name %></div>
        <div class="data-item"><strong>Address:</strong> <%= formData.complete_address %></div>

        <form action="/register/submit" method="POST">
            <div class="actions">
                <button type="submit">Confirm & Register</button>
                <a href="/register/step1">Back to Skck</a>
            </div>
        </form>
    <% } else { %>
        <p>No registration data found. Please start from <a href="/register/step1">Step 1</a>.</p>
    <% } %>
</body>
</html>